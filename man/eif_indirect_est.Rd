% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_eif.R
\name{eif_indirect_est}
\alias{eif_indirect_est}
\title{EIF-based estimator of the indirect effect}
\usage{
eif_indirect_est(
  dataset,
  a = "a",
  b = "b",
  c1 = "sc1",
  c2 = "sc2",
  clamp = c(1e-06, 1 - 1e-06),
  nuisance_type = c("simple", "flexible")
)
}
\arguments{
\item{dataset}{A data frame containing the observed data. Must include
variables \code{y}, \code{treatment}, \code{s}, \code{L1}, and \code{L2}.}

\item{a}{Character value identifying the active treatment in trial 1.}

\item{b}{Character value identifying the active treatment in trial 2.}

\item{c1}{Character value identifying the standard-of-care treatment
in trial 1.}

\item{c2}{Character value identifying the standard-of-care treatment
in trial 2.}

\item{clamp}{Numeric vector of length two specifying lower and upper
truncation bounds for estimated probabilities.}

\item{nuisance_type}{Character string indicating nuisance estimation
strategy. Must be one of \code{"simple"} or \code{"flexible"}.}
}
\value{
A list containing:
\describe{
\item{indirect}{Estimated indirect effect.}
\item{se_if}{Influence-function-based standard error.}
\item{CI_if}{Wald-type 95\\% confidence interval.}
\item{eif}{Estimated efficient influence function values.}
\item{theta}{Estimated pathway-specific component \eqn{\theta}.}
\item{phi}{Estimated direct-effect component \eqn{\phi}.}
\item{nuisance_type}{Nuisance estimation strategy used.}
}
}
\description{
Estimates the indirect effect by combining the EIF-based estimator of
the pathway-specific contrast \eqn{\theta} with the EIF-based estimator
of the direct contrast \eqn{\phi}.
}
\details{
Specifically,
\deqn{
\psi_{\mathrm{indirect}}
=
\theta + \phi.
}

The indirect effect is estimated as the sum of:
\itemize{
\item the EIF-based estimator of \eqn{\theta}, obtained from
\code{\link[=eif_theta_est]{eif_theta_est()}}, and
\item the EIF-based estimator of the direct contrast \eqn{\phi},
obtained from \code{\link[=eif_direct_est]{eif_direct_est()}}.
}

The efficient influence function for the indirect effect is constructed
as the sum of the estimated influence functions for \eqn{\theta} and
\eqn{\phi}. Standard errors and confidence intervals are then obtained
using the empirical variance of this combined EIF.
}
\examples{
\dontrun{
fit <- eif_indirect_est(
  dataset = dat,
  nuisance_type = "flexible"
)

fit$indirect
fit$CI_if
}

}
\seealso{
\code{\link[=eif_theta_est]{eif_theta_est()}}, \code{\link[=eif_direct_est]{eif_direct_est()}}
}
