# Getting Started

``` r

library(tbcompare)
```

Let’s start with a simple example: a cross-trial causal comparison
problem where we want to estimate the effect of treatment `a` versus
treatment `b` when the two randomized trials do not share the same
active comparator.

The [tbcompare](https://congjiang.github.io/tbcompare/) package provides
a common interface for fitting several estimators under different
identification strategies. These include:

- inverse probability weighting (IPW),
- g-computation,
- one-step / efficient influence function (EIF) estimation, and
- targeted minimum loss-based estimation (TMLE).

The package also supports several comparison strategies, including
traditional indirect comparison, observational comparison, direct pooled
IPD comparison, and indirect pooled IPD comparison.

## Simulating Example Data

For illustration, we will use a simulated dataset generated by
[`simulate_tb_data()`](https://CONGJIANG.github.io/tbcompare/reference/simulate_tb_data.md).
The data-generating mechanism is motivated by cross-trial tuberculosis
treatment comparisons.

``` r

dat <- simulate_tb_data(
  n = 1000,
  seed = 1
)

head(dat)
#>   s           L1 L2 country treatment y
#> 1 1  0.077303123  1       2         a 1
#> 2 2 -1.296868642  1       4       sc1 1
#> 3 1 -1.183242240  0       1       sc1 1
#> 4 1  0.011292688  0       2         a 0
#> 5 2 -0.008398964  0       2       sc2 1
#> 6 1  1.593967454  1       2       sc2 1
```

The simulated data contain the outcome, treatment assignment, study
indicator, and baseline covariates needed by the estimators in
[tbcompare](https://congjiang.github.io/tbcompare/).

## A First Analysis

The main user-facing function is
[`tb_compare()`](https://CONGJIANG.github.io/tbcompare/reference/tb_compare.md).
A simple call fits one comparison approach with one estimator.

For example, the following code fits a TMLE estimator using the direct
pooled IPD comparison approach.

``` r

fit <- tb_compare(
  data = dat,
  approach = "direct",
  estimator = "tmle",
  nuisance_type = "simple"
)

fit
#> 
#> tbcompare object
#> 
#> Estimator: TMLE 
#> Approach : Direct 
#> 
#> Estimate : 0.0699
#> SE       : 0.0515
#> 
#> 95% CI:
#> (-0.0311, 0.1708)
```

Notice what
[`tb_compare()`](https://CONGJIANG.github.io/tbcompare/reference/tb_compare.md)
returns: an object containing the point estimate, standard error,
confidence interval, and information about the fitted approach and
estimator.

We can store the fitted object and summarize it:

``` r

summary(fit)
#>   estimator approach   estimate         se      ci_low  ci_high
#> 1      tmle   direct 0.06989435 0.05150586 -0.03105529 0.170844
```

## Reusing Arguments

When fitting several estimators to the same dataset, it is often helpful
to store repeated arguments first. This keeps the calls short and makes
the analysis easier to read.

``` r

comparison_data <- dat
comparison_approach <- "direct"

fit_tmle <- tb_compare(
  data = comparison_data,
  approach = comparison_approach,
  estimator = "tmle",
  nuisance_type = "simple"
)

fit_tmle
#> 
#> tbcompare object
#> 
#> Estimator: TMLE 
#> Approach : Direct 
#> 
#> Estimate : 0.0699
#> SE       : 0.0515
#> 
#> 95% CI:
#> (-0.0311, 0.1708)
```

## Trying Different Estimators

We can compare several estimators under the same identification
strategy.

``` r

fit_ipw <- tb_compare(
  data = comparison_data,
  approach = comparison_approach,
  estimator = "ipw",
  nuisance_type = "simple"
)

fit_gcomp <- tb_compare(
  data = comparison_data,
  approach = comparison_approach,
  estimator = "gcomp",
  nuisance_type = "simple"
)

fit_onestep <- tb_compare(
  data = comparison_data,
  approach = comparison_approach,
  estimator = "onestep",
  nuisance_type = "simple"
)

fit_tmle <- tb_compare(
  data = comparison_data,
  approach = comparison_approach,
  estimator = "tmle",
  nuisance_type = "simple"
)
```

The fitted objects can then be printed or summarized individually.

``` r

fit_ipw
#> 
#> tbcompare object
#> 
#> Estimator: IPW 
#> Approach : Direct 
#> 
#> Estimate : 0.0454
#> SE       : 0.0779
#> 
#> 95% CI:
#> (-0.1074, 0.1981)
fit_gcomp
#> 
#> tbcompare object
#> 
#> Estimator: GCOMP 
#> Approach : Direct 
#> 
#> Estimate : 0.0926
#> SE       : 0.0452
#> 
#> 95% CI:
#> (0.0098, 0.1844)
fit_onestep
#> 
#> tbcompare object
#> 
#> Estimator: ONESTEP 
#> Approach : Direct 
#> 
#> Estimate : 0.0812
#> SE       : 0.0508
#> 
#> 95% CI:
#> (-0.0185, 0.1808)
fit_tmle
#> 
#> tbcompare object
#> 
#> Estimator: TMLE 
#> Approach : Direct 
#> 
#> Estimate : 0.0699
#> SE       : 0.0515
#> 
#> 95% CI:
#> (-0.0311, 0.1708)
```

## Trying Different Comparison Approaches

The same estimator can also be used with different comparison
approaches.

``` r

fit_traditional <- tb_compare(
  data = comparison_data,
  approach = "traditional",
  estimator = "tmle",
  nuisance_type = "simple"
)

fit_observational <- tb_compare(
  data = comparison_data,
  approach = "observational",
  estimator = "tmle",
  nuisance_type = "simple"
)

fit_direct <- tb_compare(
  data = comparison_data,
  approach = "direct",
  estimator = "tmle",
  nuisance_type = "simple"
)

fit_indirect <- tb_compare(
  data = comparison_data,
  approach = "indirect",
  estimator = "tmle",
  nuisance_type = "simple"
)
```

``` r

fit_traditional
#> 
#> tbcompare object
#> 
#> Estimator: TMLE 
#> Approach : Traditional 
#> 
#> Estimate : 0.0678
#> SE       : 0.0725
#> 
#> 95% CI:
#> (-0.0743, 0.2099)
fit_observational
#> 
#> tbcompare object
#> 
#> Estimator: TMLE 
#> Approach : Observational 
#> 
#> Estimate : 0.0773
#> SE       : 0.0492
#> 
#> 95% CI:
#> (-0.0191, 0.1738)
fit_direct
#> 
#> tbcompare object
#> 
#> Estimator: TMLE 
#> Approach : Direct 
#> 
#> Estimate : 0.0699
#> SE       : 0.0515
#> 
#> 95% CI:
#> (-0.0311, 0.1708)
fit_indirect
#> 
#> tbcompare object
#> 
#> Estimator: TMLE 
#> Approach : Indirect 
#> 
#> Estimate : 0.0699
#> SE       : 0.0515
#> 
#> 95% CI:
#> (-0.0311, 0.1708)
```

## Simple and Flexible Nuisance Estimation

Some estimators require nuisance functions, such as outcome regressions,
treatment mechanisms, or study membership probabilities. The argument
`nuisance_type` controls how these nuisance functions are estimated.

For a fast, reproducible example, we use `nuisance_type = "simple"`
above. When flexible estimation is desired, use:

``` r

fit_flexible <- tb_compare(
  data = comparison_data,
  approach = "direct",
  estimator = "tmle",
  nuisance_type = "flexible"
)

fit_flexible
```

Flexible nuisance estimation may be slower, but it can be useful when
the working models used by the simple implementation are too
restrictive.

## A Minimal Workflow

A typical [tbcompare](https://congjiang.github.io/tbcompare/) workflow
is:

1.  prepare or simulate a dataset with the required outcome, treatment,
    study, and covariate variables;
2.  choose an identification approach;
3.  choose an estimator;
4.  call
    [`tb_compare()`](https://CONGJIANG.github.io/tbcompare/reference/tb_compare.md);
    and
5.  inspect the estimate, standard error, and confidence interval.

For example:

``` r

dat <- simulate_tb_data(n = 1000, seed = 123)

fit <- tb_compare(
  data = dat,
  approach = "direct",
  estimator = "tmle",
  nuisance_type = "simple"
)

summary(fit)
#>   estimator approach   estimate         se     ci_low    ci_high
#> 1      tmle   direct -0.0285599 0.04968339 -0.1259375 0.06881775
```

## Session Information

``` r

sessionInfo()
#> R version 4.6.0 (2026-04-24)
#> Platform: x86_64-pc-linux-gnu
#> Running under: Ubuntu 24.04.4 LTS
#> 
#> Matrix products: default
#> BLAS:   /usr/lib/x86_64-linux-gnu/openblas-pthread/libblas.so.3 
#> LAPACK: /usr/lib/x86_64-linux-gnu/openblas-pthread/libopenblasp-r0.3.26.so;  LAPACK version 3.12.0
#> 
#> locale:
#>  [1] LC_CTYPE=C.UTF-8       LC_NUMERIC=C           LC_TIME=C.UTF-8       
#>  [4] LC_COLLATE=C.UTF-8     LC_MONETARY=C.UTF-8    LC_MESSAGES=C.UTF-8   
#>  [7] LC_PAPER=C.UTF-8       LC_NAME=C              LC_ADDRESS=C          
#> [10] LC_TELEPHONE=C         LC_MEASUREMENT=C.UTF-8 LC_IDENTIFICATION=C   
#> 
#> time zone: UTC
#> tzcode source: system (glibc)
#> 
#> attached base packages:
#> [1] stats     graphics  grDevices utils     datasets  methods   base     
#> 
#> other attached packages:
#> [1] tbcompare_0.0.0.9000
#> 
#> loaded via a namespace (and not attached):
#>  [1] digest_0.6.39     desc_1.4.3        R6_2.6.1          fastmap_1.2.0    
#>  [5] xfun_0.58         cachem_1.1.0      knitr_1.51        htmltools_0.5.9  
#>  [9] rmarkdown_2.31    lifecycle_1.0.5   cli_3.6.6         sass_0.4.10      
#> [13] pkgdown_2.2.0     textshaping_1.0.5 jquerylib_0.1.4   systemfonts_1.3.2
#> [17] compiler_4.6.0    tools_4.6.0       ragg_1.5.2        bslib_0.11.0     
#> [21] evaluate_1.0.5    yaml_2.3.12       jsonlite_2.0.0    rlang_1.2.0      
#> [25] fs_2.1.0          htmlwidgets_1.6.4
```
